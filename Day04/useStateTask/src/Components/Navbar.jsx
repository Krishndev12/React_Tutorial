const Navbar = () => {
  return (
    <div>
      <button onClick={()=>{
        
      }}>Dark</button>
      <button onClick={()=>{
        
      }}>Light</button>
    </div>
  );
};

export default Navbar;
